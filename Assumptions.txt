- Adding a library for the requirements that will be eventually used in an api application REST or gRPC
- Behaviours 1 to 4 im assuming can happen even before the songs are added to the system as its mostly communication between artist and record label
- Record label can change their mind for a proposed release date (and the proposed release date itself) of a new song that already exists in the system but im not going to bother with an update function for that in this assignment
- Not taking the release time in account although in real scenarios time of release may matter but it is complex for different timezones
- Artist always proposes a release date when adding songs and the agreement of the proposed release date from the record label is added for those songs
- assuming the search result to return the top song that matches the title (so the least number of substitutions, insertions and deletions)
- not worrying about updating more songs to the list of songs ready to release in this
- the report im assuming will be constructed as a file of some type after calling retrieveSongs in the SongsManagementService. Not implementing the actual file being built as all the data is available for it to be in any form deemed acceptable as a report.
- assuming each stream is payable once to the artist so once a stream is paid then we dont need to pay for that stream again in the future.
- artist might want specific songs not to be streamed